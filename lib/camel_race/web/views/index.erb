<div class="container">
  <div class="row mt-3">
    <div class="col">
      <% if @tracks.size.nonzero? %>
          <%= partial :tracks, locals: {tracks: @tracks } %>
      <% else %>
        <p class="lead">
          There is nothing in progress
        </p>
      <% end %>
    </div>
  </div>
</div>
