sudo: false
language: ruby
rvm:
  - 2.4.1
before_install: gem install bundler -v 1.16.0

services:
  - redis-server

script:
  - bundle exec rspec
  - bundle exec rubocop

notifications:
  email: false
