sudo: false
language: ruby
cache: bundler
rvm:
  - 2.2.8
  - 2.3.5
  - 2.4.2
  - ruby-head
  - jruby-head
before_install: gem install bundler -v 1.16.0

services:
  - redis-server

script:
  - bundle exec rspec
  - bundle exec rubocop

notifications:
  email: false
